# No external dependencies required - uses Ableton's built-in MIDI framework

# Previous external dependencies (no longer needed):
# - mido>=1.3.0 (replaced with Ableton's built-in MIDI framework)
# - python-rtmidi (replaced with Ableton's built-in MIDI framework)
# - typing-extensions==3.10.0.2
# - importlib-metadata==4.12.0
# - zipp>=3.0.0
# - packaging>=20.0

# DALConnector now uses Ableton Live's native MIDI framework - no external dependencies required!